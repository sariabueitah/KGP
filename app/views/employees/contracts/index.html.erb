<div class="w-full">
  <% if notice.present? %>
    <p class="py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-lg inline-block" id="notice"><%= notice %></p>
  <% end %>
  <% content_for :title, "Contracts" %>
  <%= turbo_frame_tag "load_employee_contracts" do %>
    <%= render partial: "employee_contracts_index", locals:{ employee: @employee, contracts: @contracts } %>
  <%end%>
</div>
